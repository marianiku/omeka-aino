// painiketoimintoja XML-transkriptiossa

// popup-kommentit; sivutus (edellinen- ja seuraava-painikkeet)
$(document).ready(function() {

   // hakee comments.js-listan avaimet tekstistä ja luo niille linkin ja tooltipin

      $.each(comments, function(key, value) {
         var first = $('#textFrame1').text().indexOf(key);
         if (first >= 0) {
            var last = first + key.length;
            var ext = $('#textFrame1').text().indexOf(' ', last);
            var ending = $('#textFrame1').text().substring(last, ext);
 
            if (ending.indexOf(",") >= 0) {
              ending = ending.substring(0, ending.indexOf(","));
            } else if (ending.indexOf(".") >= 0) {
              ending = ending.substring(0, ending.indexOf("."));
            } else if (ending.indexOf(";") >= 0) {
              ending = ending.substring(0, ending.indexOf(";"));
            } else if (ending.indexOf(":") >= 0) {
              ending = ending.substring(0, ending.indexOf(":"));
            }

            var str = key + ending;

            $('#textFrame1')
            .html($('#textFrame1')
            .html()
            .replace(str, '<a class="comm tooltip bt" href="#">' + str + '<span>' + value + '</span></a>'));
         }
     });

   var i = 0;

   // takaisin-painike: näyttää edellisen, piilottaa muut; laukaisee UV:n takaisin-painikkeen
   $('#btPrevXML').click(function() {
        if (i == 0) {
           return false;
        }

      	var current = $('#textFrame1').find('.page:eq(' + i + ')');
        var prev = current.prev();

        current.hide();
        if (prev) {
          prev.show().prevAll().hide();
        }

        var bt = $("div.uv.omeka-test-letters:eq(1) iframe").contents().find("div.paging.btn.prev");
        if (!prev.find('.pb').hasClass(current.find('.pb').attr('class'))) {
          bt.trigger("click");
        }
        i--;
    });

   // seuraava-painike, skrollaa seuraavan sivun kohtaan ja laukaisee kuvakehyksen seuraava-painikkeen
   $('#btNextXML').click(function() {

       if (i == $('#textFrame1').find('.pb').length-1) {
          return false;
       }

      	var current = $('#textFrame1').find('.page:eq(' + i + ')');
        var next = current.next();

        var nextBt = $("div.uv.omeka-test-letters:eq(1) iframe").contents().find("div.paging.btn.next");
        var prevBt = $("div.uv.omeka-test-letters:eq(1) iframe").contents().find("div.paging.btn.prev");

        current.hide();
        if (next) {
           next.show().siblings('.page').hide();
        }       

        if (typeof next != undefined && !next.find('.pb').hasClass(current.find('.pb').attr('class'))) {
          nextBt.trigger('click');
        } else if (typeof next == undefined) {
          return false;
        }
        i++;
     });

});
